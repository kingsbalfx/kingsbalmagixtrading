export default async function handler(req, res) {
  // Placeholder for Mailchimp webhook handling (subscribe confirmation, automation triggers)
  // You can use this to send welcome emails after successful payment verification.
  res.status(200).json({ received: true });
}
